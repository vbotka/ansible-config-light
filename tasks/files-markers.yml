---

- name: "files-markers: Mark block {{ item.1.marker }}"
  include_tasks: fn/mark-block.yml
  loop: "{{ cl_files.values()|list|subelements('markers', skip_missing=true) }}"
  loop_control:
    label: "{{ item.0.path }}"

# EOF
...
